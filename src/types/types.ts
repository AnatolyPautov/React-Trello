
export type Column = {
    id: number;
    title: string;
} 
export type Card = {
    id: string;
    title: string,
    description: string,
} 
export type Comment = {
    id: string;
    text: string,
} 